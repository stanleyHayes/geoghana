# @ghanageo/core

Dependency-free TypeScript types generated from GhanaGeo's published OpenAPI 3.1 contract.

```ts
import type { Place, SearchPage } from "@ghanageo/core";
```

Run `pnpm generate` after changing `contracts/openapi/v1.yaml`. CI rejects stale generated artifacts.
